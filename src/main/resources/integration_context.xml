<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-jdbc="http://www.springframework.org/schema/integration/jdbc"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/integration/jdbc
        http://www.springframework.org/schema/integration/jdbc/spring-integration-jdbc-5.2.xsd
        http://www.springframework.org/schema/integration
        http://www.springframework.org/schema/integration/spring-integration-5.2.xsd">

    <int:gateway id="gateway" service-interface="ru.volkov.integration.jdbc.CircuitGateway" default-request-channel="inboundChannel"/>

    <int-jdbc:outbound-gateway
        request-channel="inboundChannel"
        data-source="dataSource"
        update="insert into main.circuit (id, name) values (:payload.id, :payload.name)"
        query="select * from main.circuit as c where c.id = :payload.id"
        reply-channel="jdbcChannel"/>

    <bean id="mp" class="ru.volkov.integration.jdbc.MessagePrinter"/>

    <int:service-activator ref="mp" method="print" input-channel="jdbcChannel"/>

    <int:channel id="jdbcChannel"/>
    <int:channel id="inboundChannel"/>

</beans>